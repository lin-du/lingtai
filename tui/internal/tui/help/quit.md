# /quit

Quit lingtai-tui.

## Usage

```
/quit
```

## What it does

Exits `lingtai-tui`. Agents continue running in the background unless you
suspended them first.

## When to use it

When you're done with the TUI. If you want agents to stop too, use `/suspend
all` (or `/sleep all`) before quitting.
