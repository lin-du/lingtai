# /mailbox

Browse all mailbox messages.

## Usage

```
/mailbox
```

## What it does

Browses every mailbox message — inbox, sent, and IMAP — with the full message
body and inline attachment rendering.

## When to use it

When you want the complete mail record for an agent, including external IMAP
mail and attachments, rather than the live chat stream.
