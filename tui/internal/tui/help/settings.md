# /settings

Edit TUI preferences.

## Usage

```
/settings
```

## What it does

Opens TUI preferences: theme, mail page size, language, and the auto-insights
toggle.

## When to use it

For look-and-feel and client-side behavior of the TUI itself. To change the
agent's provider, model, or capabilities, use `/setup` instead.
