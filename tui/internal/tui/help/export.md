# /export

Export a reusable recipe.

## Usage

```
/export          # export a recipe
/export recipe   # explicit form
```

## What it does

Exports a reusable recipe of the current setup so it can be shared.

## When to use it

When you want to package your agent configuration for reuse or to publish to the
`/agora` marketplace.
