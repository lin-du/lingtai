# /molt

Force the agent to molt now.

## Usage

```
/molt
```

## What it does

Forces the agent to molt immediately: it saves its context and then resets the
conversation window.

## When to use it

When the context window is getting full but you don't want to lose what the
agent has built up. Unlike `/clear`, a molt saves context before resetting.
