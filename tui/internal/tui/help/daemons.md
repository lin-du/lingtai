# /daemons

Browse daemon runs.

## Usage

```
/daemons
```

## What it does

Opens the daemon browser: inspect per-agent daemon runs and their status, full
tasks, full `chat_history` interactions, and full tool/event records.

## When to use it

When you need to trace exactly what a background daemon did — its tasks, its
conversation, and the tool calls and events it produced.
