# /kanban

Open the agent network dashboard.

## Usage

```
/kanban
```

## What it does

Shows a dashboard of every agent in the network: properties, LLM config,
capabilities, and context usage at a glance.

## When to use it

When you want a single terminal view comparing all agents' configuration and how
full their context windows are. For a graphical, browser-based view use `/viz`.
